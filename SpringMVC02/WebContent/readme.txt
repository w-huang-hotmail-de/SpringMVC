This project is builded under Tomcat 8.0.5 (JSP 2.3 and above).  Since JSP 2.3 (the change applies to Tomcat 8 onwards) only the request method GET, POST and HEAD are supported. 
The behaviour for other HTTP method, for example PUT and DELETE, result in the error 405 - Method Not Allowed.

